<Window x:Class="PasswordManager.WinUi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource ModernBackgroundBrush}">
        <!-- Login Frame (shown during authentication) -->
        <Frame x:Name="LoginFrame"
               x:FieldModifier="private"
               Background="{StaticResource ModernBackgroundBrush}"
               Visibility="Visible"/>
               
        <!-- Main Navigation View (shown after authentication) -->
        <NavigationView x:Name="MainNavigationView"
                       Style="{StaticResource ModernNavigationViewStyle}"
                       IsBackButtonVisible="Collapsed"
                       IsSettingsVisible="False"
                       IsPaneToggleButtonVisible="True"
                       OpenPaneLength="280"
                       CompactModeThresholdWidth="640"
                       ExpandedModeThresholdWidth="1028"
                       SelectionChanged="MainNavigationView_SelectionChanged"
                       Visibility="Collapsed">
            
            <!-- Search Box -->
            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox x:Name="SearchBox"
                              QueryIcon="Find"
                              PlaceholderText="Search passwords..."
                              TextChanged="SearchBox_TextChanged"
                              QuerySubmitted="SearchBox_QuerySubmitted"
                              MinWidth="200"
                              Style="{StaticResource ModernAutoSuggestBoxStyle}"
                              CornerRadius="20"/>
            </NavigationView.AutoSuggestBox>

            <!-- Header Content -->
            <NavigationView.Header>
                <Grid Height="60" Margin="24,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- App Icon -->
                        <Border Background="{StaticResource ModernPrimaryGradientBrush}"
                               CornerRadius="12"
                               Width="40"
                               Height="40"
                               Margin="0,0,16,0">
                            <FontIcon Glyph="&#xE192;" 
                                     FontSize="20"
                                     Foreground="White"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                        
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Password Manager"
                                      Style="{StaticResource ModernHeading3Style}"/>
                            <TextBlock Text="Secure your digital life"
                                      Style="{StaticResource ModernCaptionStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </NavigationView.Header>
            
            <!-- Navigation Menu Items -->
            <NavigationView.MenuItems>
                <!-- All Accounts Header -->
                <NavigationViewItemHeader Content="All Accounts"/>
                
                <!-- Home -->
                <NavigationViewItem x:Name="HomeNavItem" 
                                  Content="Dashboard" 
                                  Tag="Home"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" FontSize="18"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItemSeparator/>
                
                <!-- Collections Header -->
                <NavigationViewItemHeader Content="COLLECTIONS"/>
                
                <!-- Collections -->
                <NavigationViewItem Content="All Items" 
                                  Tag="AllItems"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8FD;" FontSize="18" Foreground="{StaticResource ModernPrimaryBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Favorites" 
                                  Tag="Favorites"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE1CE;" FontSize="18" Foreground="{StaticResource ModernWarningBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Watchtower" 
                                  Tag="Watchtower"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7BA;" FontSize="18" Foreground="{StaticResource ModernAccentBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItemSeparator/>
                
                <!-- Categories Header -->
                <NavigationViewItemHeader Content="CATEGORIES"/>
                
                <!-- Categories -->
                <NavigationViewItem Content="All Categories" 
                                  Tag="AllCategories"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8FD;" FontSize="18" Foreground="{StaticResource ModernPrimaryBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem x:Name="PasswordsNavItem" 
                                  Content="Logins" 
                                  Tag="Passwords"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE192;" FontSize="18" Foreground="{StaticResource ModernSuccessBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Credit Cards" 
                                  Tag="CreditCards"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8C7;" FontSize="18" Foreground="{StaticResource ModernErrorBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Identities" 
                                  Tag="Identities"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE77B;" FontSize="18" Foreground="{StaticResource ModernAccentBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItemSeparator/>
                
                <!-- Tags Header -->
                <NavigationViewItemHeader Content="TAGS"/>
                
                <!-- Tags Placeholder -->
                <NavigationViewItem Content="No tags yet" 
                                  IsEnabled="False"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8EC;" FontSize="16" Foreground="{StaticResource ModernTextTertiaryBrush}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItemSeparator/>
                
                <!-- Tools Section -->
                <NavigationViewItemHeader Content="TOOLS"/>
                
                <NavigationViewItem x:Name="CategoriesNavItem" 
                                  Content="Categories" 
                                  Tag="Categories"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8FD;" FontSize="18"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem x:Name="ImportNavItem" 
                                  Content="Import Data" 
                                  Tag="Import"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B5;" FontSize="18"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem x:Name="SettingsNavItem" 
                                  Content="Settings" 
                                  Tag="Settings"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" FontSize="18"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            
            <!-- Footer Menu Items -->
            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="AboutNavItem" 
                                  Content="About" 
                                  Tag="About"
                                  Style="{StaticResource ModernNavigationViewItemStyle}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" FontSize="16"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            
            <!-- Content Frame -->
            <ScrollViewer ZoomMode="Disabled" 
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled">
                <Frame x:Name="ContentFrame" 
                       Margin="24,16,24,24"
                       Background="Transparent"/>
            </ScrollViewer>
            
        </NavigationView>
    </Grid>
</Window>