<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PasswordManager.WinUi.Controls.ReadOnlyField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignWidth="400"
    d:DesignHeight="40"
    IsTabStop="True"
    GotFocus="Root_GotFocus"
    LostFocus="Root_LostFocus">

    <UserControl.KeyboardAccelerators>
        <KeyboardAccelerator Key="C"
                Modifiers="Control"
                Invoked="CopyKeyboardAccelerator_Invoked"/>
    </UserControl.KeyboardAccelerators>

    <Grid Background="Transparent"
          PointerEntered="Root_PointerEntered"
          PointerExited="Root_PointerExited">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Displayed value -->
        <TextBlock x:Name="ValueTextBlock"
                   Text="{x:Bind Text, Mode=OneWay}"
                   TextTrimming="CharacterEllipsis"
                   VerticalAlignment="Center"
                   Foreground="{ThemeResource ModernTextPrimaryBrush}"
                   FontFamily="{x:Bind FontFamilyOverride, Mode=OneWay}"/>

        <!-- Copy button - appears when hovered -->
        <Button x:Name="CopyButton"
                Grid.Column="1"
                Visibility="Collapsed"
                Click="CopyButton_Click"
                Margin="8,0,0,0"
                Padding="12,6"
                HorizontalAlignment="Right"
                VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal"
                        Spacing="8">
                <TextBlock Text="Copy"
                           Foreground="White"
                           FontWeight="SemiBold"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
