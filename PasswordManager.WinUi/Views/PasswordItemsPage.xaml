<Page x:Class="PasswordManager.WinUi.Views.PasswordItemsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource ModernBackgroundBrush}">

    <ScrollViewer>
        <StackPanel Spacing="24" Margin="0,0,0,24">
            
            <!-- Page Header -->
            <Border Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Background="{StaticResource ModernPrimaryGradientBrush}"
                                   CornerRadius="12"
                                   Width="32"
                                   Height="32">
                                <FontIcon Glyph="&#xE192;" 
                                         FontSize="16"
                                         Foreground="White"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Password Vault" 
                                      Style="{StaticResource ModernHeading3Style}"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Manage and organize your secure passwords"
                                  Style="{StaticResource ModernCaptionStyle}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                        <Button x:Name="RefreshButton"
                               Style="{StaticResource ModernSecondaryButtonStyle}"
                               Click="RefreshButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                <TextBlock Text="Refresh"/>
                            </StackPanel>
                        </Button>
                        
                        <Button x:Name="AddButton"
                               Style="{StaticResource ModernPrimaryButtonStyle}"
                               Click="AddButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE109;" FontSize="14"/>
                                <TextBlock Text="Add Password"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Search and Filters -->
            <Border Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Search Box -->
                    <StackPanel Grid.Column="0" Spacing="8" Margin="0,0,20,0">
                        <TextBlock Text="Search" 
                                  Style="{StaticResource ModernCaptionStyle}"
                                  FontWeight="SemiBold"/>
                        <TextBox x:Name="SearchTextBox"
                                PlaceholderText="🔍 Search passwords..."
                                Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                TextChanged="SearchTextBox_TextChanged"
                                Style="{StaticResource ModernTextBoxStyle}"/>
                    </StackPanel>
                    
                    <!-- Type Filter -->
                    <StackPanel Grid.Column="1" Spacing="8" Margin="10,0">
                        <TextBlock Text="Type" 
                                  Style="{StaticResource ModernCaptionStyle}"
                                  FontWeight="SemiBold"/>
                        <ComboBox PlaceholderText="All Types"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 HorizontalAlignment="Stretch">
                            <ComboBoxItem Content="Login"/>
                            <ComboBoxItem Content="Credit Card"/>
                            <ComboBoxItem Content="Secure Note"/>
                            <ComboBoxItem Content="WiFi"/>
                        </ComboBox>
                    </StackPanel>
                    
                    <!-- Category Filter -->
                    <StackPanel Grid.Column="2" Spacing="8" Margin="20,0,0,0">
                        <TextBlock Text="Category" 
                                  Style="{StaticResource ModernCaptionStyle}"
                                  FontWeight="SemiBold"/>
                        <ComboBox PlaceholderText="All Categories"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 HorizontalAlignment="Stretch">
                            <ComboBoxItem Content="Work"/>
                            <ComboBoxItem Content="Personal"/>
                            <ComboBoxItem Content="Finance"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Password Items Content -->
            <Grid MinHeight="400">
                <!-- Loading Indicator -->
                <Border Style="{StaticResource ModernCardStyle}"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Padding="48"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Spacing="20" HorizontalAlignment="Center">
                        <ProgressRing IsActive="True" Width="48" Height="48"/>
                        <TextBlock Text="Loading your passwords..." 
                                  Style="{StaticResource ModernBodyStyle}"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- Password Items ListView -->
                <ListView x:Name="PasswordItemsList"
                         ItemsSource="{Binding PasswordItems}"
                         Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                         DoubleTapped="PasswordItemsList_DoubleTapped"
                         SelectionMode="None">
                    
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Type Icon -->
                                    <Border Grid.Column="0"
                                           Background="{StaticResource ModernPrimaryGradientBrush}"
                                           CornerRadius="10"
                                           Width="40"
                                           Height="40"
                                           Margin="0,0,16,0"
                                           VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Type, Converter={StaticResource TypeToIconConverter}}"
                                                  FontSize="18"
                                                  Foreground="White"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <!-- Password Info -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                        <TextBlock Text="{Binding Title}" 
                                                  Style="{StaticResource ModernBodyStyle}"
                                                  FontWeight="SemiBold"
                                                  TextTrimming="CharacterEllipsis"/>
                                        
                                        <TextBlock Text="{Binding Description}" 
                                                  Style="{StaticResource ModernCaptionStyle}"
                                                  TextTrimming="CharacterEllipsis"
                                                  Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        
                                        <TextBlock Text="{Binding Username}" 
                                                  Style="{StaticResource ModernCaptionStyle}"
                                                  TextTrimming="CharacterEllipsis"
                                                  Visibility="{Binding Username, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                    
                                    <!-- Status Indicators -->
                                    <StackPanel Grid.Column="2" 
                                               Orientation="Horizontal" 
                                               Spacing="8"
                                               VerticalAlignment="Center"
                                               Margin="0,0,16,0">
                                        <!-- Favorite Star -->
                                        <Border Background="{StaticResource ModernWarningBrush}"
                                               CornerRadius="8"
                                               Padding="6"
                                               Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <FontIcon Glyph="&#xE1CE;" 
                                                     FontSize="12"
                                                     Foreground="White"/>
                                        </Border>
                                    </StackPanel>
                                    
                                    <!-- Action Menu -->
                                    <Button Grid.Column="3" 
                                           Background="Transparent"
                                           BorderThickness="0"
                                           Padding="12"
                                           CornerRadius="8">
                                        <FontIcon Glyph="&#xE712;" FontSize="16" Foreground="{StaticResource ModernTextSecondaryBrush}"/>
                                        <Button.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem Click="EditMenuItem_Click">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE70F;"/>
                                                    </MenuFlyoutItem.Icon>
                                                    <MenuFlyoutItem.Text>Edit</MenuFlyoutItem.Text>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutSeparator/>
                                                <MenuFlyoutItem>
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE8C8;"/>
                                                    </MenuFlyoutItem.Icon>
                                                    <MenuFlyoutItem.Text>Copy Password</MenuFlyoutItem.Text>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem>
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE8C8;"/>
                                                    </MenuFlyoutItem.Icon>
                                                    <MenuFlyoutItem.Text>Copy Username</MenuFlyoutItem.Text>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutSeparator/>
                                                <MenuFlyoutItem>
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE1CE;"/>
                                                    </MenuFlyoutItem.Icon>
                                                    <MenuFlyoutItem.Text>Add to Favorites</MenuFlyoutItem.Text>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem>
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE7B8;"/>
                                                    </MenuFlyoutItem.Icon>
                                                    <MenuFlyoutItem.Text>Archive</MenuFlyoutItem.Text>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutSeparator/>
                                                <MenuFlyoutItem Click="DeleteMenuItem_Click" Foreground="{StaticResource ModernErrorBrush}">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE74D;" Foreground="{StaticResource ModernErrorBrush}"/>
                                                    </MenuFlyoutItem.Icon>
                                                    <MenuFlyoutItem.Text>Delete</MenuFlyoutItem.Text>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </Button.Flyout>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                
                <!-- Empty State -->
                <Border Style="{StaticResource ModernElevatedCardStyle}"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Padding="48"
                       Visibility="{Binding HasNoItems, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Spacing="20" HorizontalAlignment="Center">
                        <Border Background="{StaticResource ModernPrimaryGradientBrush}"
                               CornerRadius="24"
                               Width="80"
                               Height="80"
                               HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE192;" 
                                     FontSize="36"
                                     Foreground="White"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                        
                        <StackPanel Spacing="8" HorizontalAlignment="Center">
                            <TextBlock Text="No passwords found" 
                                      Style="{StaticResource ModernHeading3Style}"
                                      HorizontalAlignment="Center"/>
                            <TextBlock Text="Create your first password item to get started" 
                                      Style="{StaticResource ModernCaptionStyle}"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                        
                        <Button Content="Add Your First Password" 
                               Style="{StaticResource ModernPrimaryButtonStyle}"
                               Click="AddButton_Click">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE109;" FontSize="16"/>
                                    <TextBlock Text="Add Your First Password"/>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>