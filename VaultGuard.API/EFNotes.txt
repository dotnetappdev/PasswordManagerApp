>>> See "EF TOOLS.txt" (same folder) for the full, current SQLite + SQL Server
>>> instructions. NOTE: `dotnet ef ... --startup-project VaultGuard.API` (line
>>> below) silently uses SQLite via VaultGuard.DAL's design-time factory — for
>>> SQL Server use --project AND --startup-project = VaultGuard.DAL.SqlServer.

Run from the solution root (E:\sassproducts\passwordmanagermergec\PasswordManagerApp).

dotnet ef migrations add FirstMigration --context VaultGuardDbContext --project VaultGuard.DAL.SqlServer --startup-project VaultGuard.DAL.SqlServer

# 2) UPDATE the database (apply migrations). Two ways:

# 2a) RECOMMENDED - via the API, which pulls the real connection string from Google Secret
#     Manager. Set the environment first: Development or Production.
$env:ASPNETCORE_ENVIRONMENT = "Development"    # dev project  project-e96017a0-d8f6-420f-955
# 
$env:ASPNETCORE_ENVIRONMENT = "Production"  
dotnet run --project VaultGuard.API -- --migrate

 dotnet ef database update --project VaultGuard.DAL.SqlServer --startup-project VaultGuard.API
