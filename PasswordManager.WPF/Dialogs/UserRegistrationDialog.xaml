<ui:ContentDialog x:Class="PasswordManager.WinUi.Dialogs.UserRegistrationDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:converters="clr-namespace:PasswordManager.WPF.Converters"
               Title="Create New User Account"
               PrimaryButtonText="Create Account"
               SecondaryButtonText="Cancel"
               DefaultButton="Primary"
               Background="#404040"
               RequestedTheme="Dark">

    <ui:ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </ui:ContentDialog.Resources>

    <ScrollViewer MaxHeight="600" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="20" Margin="0,20,0,0">
            
            <!-- ui:InfoBar for validation messages -->
            <ui:InfoBar x:Name="Validationui:InfoBar"
                     x:FieldModifier="private"
                     IsOpen="False"
                     Severity="Warning"
                     Title="Validation Error"
                     Message=""
                     IsClosable="True"/>
            
            <!-- Personal Information Section -->
            <Border Background="#2D2D2D" 
                    CornerRadius="8" 
                    Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="Personal Information"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                    
                    <!-- First Name -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="First Name" Foreground="#B0B0B0" FontSize="14"/>
                        <Border x:Name="FirstNameBorder"
                                x:FieldModifier="private"
                                Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <TextBox x:Name="FirstNameTextBox"
                                    x:FieldModifier="private"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="White"
                                    PlaceholderText="Enter first name"
                                    TextChanged="FirstNameTextBox_TextChanged"/>
                        </Border>
                    </StackPanel>

                    <!-- Last Name -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Last Name" Foreground="#B0B0B0" FontSize="14"/>
                        <Border x:Name="LastNameBorder"
                                x:FieldModifier="private"
                                Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <TextBox x:Name="LastNameTextBox"
                                    x:FieldModifier="private"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="White"
                                    PlaceholderText="Enter last name"
                                    TextChanged="LastNameTextBox_TextChanged"/>
                        </Border>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Email Address" Foreground="#B0B0B0" FontSize="14"/>
                        <Border x:Name="EmailBorder"
                                x:FieldModifier="private"
                                Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <TextBox x:Name="EmailTextBox"
                                    x:FieldModifier="private"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="White"
                                    PlaceholderText="Enter email address"
                                    TextChanged="EmailTextBox_TextChanged"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Account Security Section -->
            <Border Background="#2D2D2D" 
                    CornerRadius="8" 
                    Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="Account Security"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                    
                    <!-- Master Password -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Master Password" Foreground="#B0B0B0" FontSize="14"/>
                        <Border x:Name="MasterPasswordBorder"
                                x:FieldModifier="private"
                                Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <PasswordBox x:Name="MasterPasswordBox"
                                        x:FieldModifier="private"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="White"
                                        PlaceholderText="Create a strong master password"
                                        PasswordChanged="MasterPasswordBox_PasswordChanged"/>
                        </Border>
                        <!-- Password Strength Indicator -->
                        <StackPanel x:Name="PasswordStrengthPanel"
                                   x:FieldModifier="private"
                                   Spacing="4"
                                   Visibility="Collapsed">
                            <ProgressBar x:Name="PasswordStrengthBar"
                                        x:FieldModifier="private"
                                        Height="4"
                                        Minimum="0"
                                        Maximum="100"
                                        Value="0"/>
                            <TextBlock x:Name="PasswordStrengthText"
                                      x:FieldModifier="private"
                                      FontSize="12"
                                      Foreground="#B0B0B0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Confirm Master Password -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Confirm Master Password" Foreground="#B0B0B0" FontSize="14"/>
                        <Border x:Name="ConfirmPasswordBorder"
                                x:FieldModifier="private"
                                Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <PasswordBox x:Name="ConfirmPasswordBox"
                                        x:FieldModifier="private"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="White"
                                        PlaceholderText="Confirm your master password"
                                        PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
                        </Border>
                    </StackPanel>

                    <!-- Password Hint -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Password Hint (Optional)" Foreground="#B0B0B0" FontSize="14"/>
                        <Border Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <TextBox x:Name="PasswordHintTextBox"
                                    x:FieldModifier="private"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="White"
                                    PlaceholderText="Enter a hint to help remember your password"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Account Role Section -->
            <Border Background="#2D2D2D" 
                    CornerRadius="8" 
                    Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="Account Role"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                    
                    <!-- Admin Toggle -->
                    <Border Background="#3A3A3A"
                            CornerRadius="8"
                            BorderBrush="#4A4A4A"
                            BorderThickness="1"
                            Padding="16,12"
                            Visibility="{x:Bind CanCreateAdminAccount, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Administrator Account"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           FontSize="16"/>
                                <TextBlock Text="Full access to all features and user management"
                                           Foreground="#B0B0B0"
                                           FontSize="13"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                            
                            <ToggleSwitch x:Name="AdminToggleSwitch"
                                         x:FieldModifier="private"
                                         Grid.Column="1"
                                         VerticalAlignment="Center"
                                         Toggled="AdminToggleSwitch_Toggled"/>
                        </Grid>
                    </Border>

                    <!-- Role Selection ComboBox (when not admin) -->
                    <StackPanel Spacing="8" 
                                Visibility="{x:Bind ShowRoleSelection, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="User Role" Foreground="#B0B0B0" FontSize="14"/>
                        <Border Background="#3A3A3A"
                                CornerRadius="8"
                                BorderBrush="#4A4A4A"
                                BorderThickness="1"
                                Padding="12">
                            <ComboBox x:Name="RoleComboBox"
                                     x:FieldModifier="private"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="White"
                                     HorizontalAlignment="Stretch"
                                     SelectionChanged="RoleComboBox_SelectionChanged">
                                <ComboBoxItem Content="Parent User" Tag="Parent"/>
                                <ComboBoxItem Content="Standard User" Tag="User" IsSelected="True"/>
                                <ComboBoxItem Content="Child User" Tag="Child"/>
                            </ComboBox>
                        </Border>
                    </StackPanel>

                    <!-- Role Description -->
                    <Border Background="#1E3A5F"
                            CornerRadius="8"
                            BorderBrush="#4A90E2"
                            BorderThickness="1"
                            Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE946;" 
                                     FontSize="16" 
                                     Foreground="#4A90E2"
                                     VerticalAlignment="Top"/>
                            <TextBlock x:Name="RoleDescriptionTextBlock"
                                      x:FieldModifier="private"
                                      Text="Standard users can manage their own passwords and access all features."
                                      Foreground="#B0D4FF"
                                      TextWrapping="Wrap"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Error Message -->
            <Border x:Name="ErrorMessageBorder"
                    x:FieldModifier="private"
                    Background="#5D2A2A"
                    BorderBrush="#DC2626"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16,12"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE783;"
                              FontSize="16"
                              Foreground="#DC2626"/>
                    <TextBlock x:Name="ErrorMessageTextBlock"
                               x:FieldModifier="private"
                               Foreground="#FCA5A5"
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</ui:ContentDialog>
