<Window xmlns:ui="http://schemas.modernwpf.com/2019"
    x:Class="PasswordManager.WinUi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource ModernBackgroundBrush}">
        <!-- Login Frame (shown during authentication) -->
        <Frame x:Name="LoginFrame"
               x:FieldModifier="private"
               Background="{StaticResource ModernBackgroundBrush}"
               Visibility="Visible"/>

        <!-- Main Navigation View (shown after authentication) -->
        <ui:NavigationView x:Name="MainNavigationView"
                        Style="{StaticResource ModernNavigationViewStyle}"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        IsPaneToggleButtonVisible="False"
                        PaneDisplayMode="Left"
                        OpenPaneLength="280"
                        CompactModeThresholdWidth="0"
                        ExpandedModeThresholdWidth="0"
                        SelectionChanged="MainNavigationView_SelectionChanged"
                        Visibility="Collapsed"
                        Margin="0"
                        Padding="0">

            <!-- Custom Pane Header with Profile -->
            <ui:NavigationView.PaneHeader>
                <Grid Margin="16,12">
                    <!-- Profile Section -->
                    <Border Grid.Row="0"
                            Background="Transparent"
                            CornerRadius="8"
                            Padding="12,8">
                        <Button x:Name="ProfileButton"
                                Background="Transparent"
                                BorderThickness="0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                Click="ProfileButton_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Profile Picture -->
                                <Border Grid.Column="0"
                                        Width="32"
                                        Height="32"
                                        CornerRadius="16"
                                        Background="{StaticResource ModernPrimaryGradientBrush}"
                                        Margin="0,0,12,0">
                                    <TextBlock Text="👤"
                                               FontSize="16"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>

                                <!-- User Info -->
                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Margin="2">
                                    <TextBlock x:Name="UserNameText"
                                               Text="David Smith"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource ModernTextPrimaryBrush}"/>
                                    <TextBlock Text="Personal"
                                               FontSize="12"
                                               Foreground="{StaticResource ModernTextSecondaryBrush}"/>
                                </StackPanel>

                                <!-- Dropdown Arrow -->
                                <FontIcon Grid.Column="2"
                                          Glyph="&#xE019;"
                                          FontSize="12"
                                          Foreground="{StaticResource ModernTextSecondaryBrush}"/>
                            </Grid>
                        </Button>
                    </Border>
                </Grid>
            </ui:NavigationView.PaneHeader>

            <!-- Header Content with Search Box -->
            <ui:NavigationView.Header>
                <Grid Margin="0">
                    <ui:AutoSuggestBox x:Name="SearchBox"
                                    QueryIcon="Find"
                                    ui:ControlHelper.PlaceholderText="Search passwords..."
                                    TextChanged="SearchBox_TextChanged"
                                    QuerySubmitted="SearchBox_QuerySubmitted"
                                    Style="{StaticResource Modernui:AutoSuggestBoxStyle}"
                                    CornerRadius="8"
                                    Height="36"
                                    HorizontalAlignment="Stretch"/>
                </Grid>
            </ui:NavigationView.Header>

            <!-- Navigation Menu Items -->
            <ui:NavigationView.MenuItems>
                <!-- All Items -->
                <ui:ui:NavigationViewItem x:Name="AllItemsNavItem"
                                    Content="All Items"
                                    Tag="AllItems"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}"
                                    RightTapped="NavigationItem_RightTapped">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8A5;"
                                  FontSize="18"
                                  Foreground="{StaticResource ModernPrimaryBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                    <ui:ui:NavigationViewItem.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Edit"
                                            Icon="Edit"
                                            Click="EditItem_Click"
                                            Tag="AllItems"/>
                            <MenuFlyoutItem Text="Delete"
                                            Icon="Delete"
                                            Click="DeleteItem_Click"
                                            Tag="AllItems"/>
                        </MenuFlyout>
                    </ui:ui:NavigationViewItem.ContextFlyout>
                </ui:ui:NavigationViewItem>

                <!-- Favorites -->
                <ui:ui:NavigationViewItem x:Name="FavoritesNavItem"
                                    Content="Favorites"
                                    Tag="Favorites"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}"
                                    RightTapped="NavigationItem_RightTapped">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE734;"
                                  FontSize="18"
                                  Foreground="{StaticResource ModernWarningBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                    <ui:ui:NavigationViewItem.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Edit"
                                            Icon="Edit"
                                            Click="EditItem_Click"
                                            Tag="Favorites"/>
                            <MenuFlyoutItem Text="Delete"
                                            Icon="Delete"
                                            Click="DeleteItem_Click"
                                            Tag="Favorites"/>
                        </MenuFlyout>
                    </ui:ui:NavigationViewItem.ContextFlyout>
                </ui:ui:NavigationViewItem>

                <!-- Personal (use the same PasswordItemsPage as other categories) -->
                <ui:ui:NavigationViewItem Content="Personal"
                                    Tag="Passwords"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#x25A0;"
                                  FontSize="16"
                                  Foreground="#3b82f6"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>

                <!-- Logins moved here to be closer to Personal -->
                <ui:ui:NavigationViewItem x:Name="LoginCategoryNavItem"
                                    Content="Logins"
                                    Tag="LoginCategory"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE192;"
                                  FontSize="16"
                                  Foreground="#3b82f6"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>

                <ui:ui:ui:NavigationViewItemSeparator Margin="12,8"/>

                <!-- Categories Section with Management Controls -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="CATEGORIES"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource ModernTextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="16,8"/>

                    <!-- Add Category Button -->
                    <Button Grid.Column="1"
                            x:Name="AddCategoryButton"
                            Width="28"
                            Height="28"
                            Margin="4,0"
                            Background="Transparent"
                            Foreground="{StaticResource ModernSuccessBrush}"
                            BorderThickness="1"
                            BorderBrush="{StaticResource ModernSuccessBrush}"
                            CornerRadius="14"
                            ToolTipService.ToolTip="Add Category"
                            Click="AddCategoryNavButton_Click">
                        <FontIcon Glyph="&#xE710;"
                                  FontSize="14"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>
                    </Button>
                    
                    <!-- Configure Categories Button -->
                    <Button Grid.Column="2"
                            x:Name="ConfigureCategoriesButton"
                            Width="28"
                            Height="28"
                            Margin="4,0"
                            Background="Transparent"
                            Foreground="{StaticResource ModernTextSecondaryBrush}"
                            BorderThickness="1"
                            BorderBrush="{StaticResource ModernBorderBrush}"
                            CornerRadius="14"
                            ToolTipService.ToolTip="Configure Favorite Categories"
                            Click="ConfigureCategoriesButton_Click">
                        <FontIcon Glyph="&#xE713;"
                                  FontSize="14"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>
                    </Button>
                </Grid>

                <!-- Dynamic Favorite Categories -->
                <StackPanel x:Name="FavoriteCategoriesPanel">
                    <!-- Favorite categories will be loaded here dynamically -->
                </StackPanel>

                <ui:ui:ui:NavigationViewItemSeparator Margin="12,8"/>

                <!-- Tags Section with Icons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="TAGS"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource ModernTextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="16,8"/>

                    <!-- Add/Delete tag buttons removed for a cleaner UI -->
                </Grid>

                <!-- Tags list rendered as chips -->
                <StackPanel x:Name="SidebarTagsPanel"
                            Orientation="Vertical"
                            Margin="12,4"
                            Margin="6">
                    <!-- Tags will be populated at runtime as rounded chips -->
                </StackPanel>

                <ui:ui:ui:NavigationViewItemSeparator Margin="12,8"/>

                <!-- Tools & Security -->
                <ui:ui:NavigationViewItem x:Name="SecurityAuditNavItem"
                                    Content="Security Dashboard"
                                    Tag="SecurityDashboard"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE72E;"
                                  FontSize="18"
                                  Foreground="{StaticResource ModernErrorBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>

                <!-- Vaults -->
                <ui:ui:NavigationViewItem x:Name="VaultsNavItem"
                                    Content="Vaults"
                                    Tag="Vaults"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE72E;"
                                  FontSize="18"
                                  Foreground="{StaticResource ModernPrimaryBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>

                <!-- Import -->
                <ui:ui:NavigationViewItem x:Name="ImportNavItem"
                                    Content="Import Data"
                                    Tag="Import"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B5;"
                                  FontSize="18"
                                  Foreground="{StaticResource ModernSuccessBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>

            <!-- Footer Menu Items -->
            <ui:NavigationView.FooterMenuItems>
                <!-- Manage Items -->
                <!--<ui:ui:NavigationViewItem x:Name="ManageAllItemsNavItem"
                                    Content="Manage Collections, Types &amp; Tags"
                                    Tag="ManageItems"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8FD;"
                                  FontSize="16"
                                  Foreground="{StaticResource ModernAccentBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>-->

                <ui:ui:ui:NavigationViewItemSeparator Margin="8,4"/>

                <ui:ui:NavigationViewItem x:Name="ArchiveNavItem"
                                    Content="Archive"
                                    Tag="Archive"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7B8;"
                                  FontSize="16"
                                  Foreground="{StaticResource ModernTextSecondaryBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>

                <ui:ui:NavigationViewItem x:Name="RecentlyDeletedNavItem"
                                    Content="Recently Deleted"
                                    Tag="RecentlyDeleted"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE74D;"
                                  FontSize="16"
                                  Foreground="{StaticResource ModernTextSecondaryBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>

                <ui:ui:ui:NavigationViewItemSeparator Margin="8,4"/>

                <ui:ui:NavigationViewItem x:Name="SettingsNavItem"
                                    Content="Settings"
                                    Tag="Settings"
                                    Style="{StaticResource Modernui:ui:NavigationViewItemStyle}">
                    <ui:ui:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;"
                                  FontSize="16"
                                  Foreground="{StaticResource ModernTextSecondaryBrush}"/>
                    </ui:ui:NavigationViewItem.Icon>
                </ui:ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>

            <!-- Content Frame -->
            <ScrollViewer ZoomMode="Disabled"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Margin="0">
                <Frame x:Name="ContentFrame"
                       Background="Transparent"
                       Margin="0"/>
            </ScrollViewer>

        </ui:NavigationView>
    </Grid>
</Window>
